# `mesh/` — reserved for Phase 4 (Mesh Editor)

Intentionally empty in Phase 1. Inspecting/editing mesh geometry, normals,
vertices, UV mapping, LOD levels, materials and bone assignments requires the
`.ydd` binary reader referenced in `docs/FILE_FORMATS.md`, plus the Phase 3
preview pipeline to render results. Both are prerequisites this directory is
waiting on.
